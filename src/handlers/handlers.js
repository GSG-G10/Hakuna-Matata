const homeHandler = require('./homeHandler');
const publicHandler = require('./publicHandler');
const searchHandler = require('./searchHandler');

const handlers = {
  homeHandler, publicHandler, searchHandler,
};

module.exports = handlers;
